openapi: 3.0.3
info:
  title: opid-edge-cases
  version: 0.1.0
  description: Synthetic spec exercising non-idiomatic operationId shapes
paths:
  /accounts/{a}/bank_accounts/{id}:
    delete:
      operationId: DeleteAccountsAccountBankAccountsId
      parameters:
        - in: path
          name: a
          required: true
          schema: {type: string}
        - in: path
          name: id
          required: true
          schema: {type: string}
      responses:
        '204':
          description: deleted
  /accounts/{a}:
    get:
      operationId: GetAccountsAccount
      parameters:
        - in: path
          name: a
          required: true
          schema: {type: string}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object}
  /account_links:
    post:
      operationId: PostAccountLinks
      requestBody:
        required: true
        content:
          application/json:
            schema: {type: object}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object}
  /chat/completions:
    post:
      operationId: createChatCompletion
      requestBody:
        required: true
        content:
          application/json:
            schema: {type: object}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object}
  /blocks/{id}:
    get:
      operationId: retrieveABlock
      parameters:
        - in: path
          name: id
          required: true
          schema: {type: string}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object}
  /repos:
    get:
      operationId: repos/list-for-user
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object}
